#include "main.h"
/**
 * _isdigit -> A function that checks for a digit (0 through 9).
 *
 * @c: An input integer
 *
 * Return: 1 if c is a gigit or 0 otherwise
 */
int _isdigit(int c)
{
	char x = '0';
	int _isdigit = 0;

	for (; x <= '9'; x++)
	{
		if (i == c)
		{
			isdigit = 1;
			break;
		}
	}

	return (isdigit);
}
